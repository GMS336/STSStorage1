@model IEnumerable<STSStorage1.Models.InvStatusModel>

@{
    ViewBag.Title = "Status";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="fletter" style="text-align:center">
    <h3>Classification List</h3>
</div>

<button type="button" class="btn btn-sm btn-success" data-bs-toggle="modal" onclick="RenderActions('/InvStatus/StatusCreate')" data-bs-target="#modalCreate">
    Add New Classification
</button>

<table class="table table-striped table-bordered table-sm" ">
    <thead class="table-dark">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ItemStatusDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td style="cursor:pointer"
                data-bs-toggle="modal" onclick="RenderActions('/InvStatus/StatusDetails/' + @item.ItemStatusID)"
                data-bs-target="#modalCreate">
                @Html.DisplayFor(modelItem => item.ItemStatus)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ItemStatusDescription)
            </td>
            <td>
@*              <a style="cursor:pointer" class="bi-pencil" asp-action="ClassEdit" asp-route-id="@item.ClassificationID"></a> |
                <a class="bi-info" asp-action="Details" asp-route-id="@item.ClassificationID"></a> |
                <a style="cursor:pointer" class="bi-trash" asp-action="Delete" asp-route-id="@item.ClassificationID"></a> *@
                <a style="cursor:pointer;" class="btn-sm btn-link bi-pencil" data-bs-toggle="modal" onclick="RenderActions('/InvStatus/StatusEdit/' + @item.ItemStatusID)" data-bs-target="#modalCreate"></a> |
                <a style="cursor:pointer;" class="btn-sm btn-link bi-trash" data-bs-toggle="modal" onclick="RenderActions('/InvStatus/StatusDelete/' + @item.ItemStatusID)" data-bs-target="#modalCreate"></a>
            
            </td>
        </tr>
}
    </tbody>
</table>

<div id="modalCreate" class="modal fade" role="dialog" data-backdrop="false">
    <div class="modal-dialog">
        <div class="modal-content modal-sm">
            <div id="OpenDialog" class="modal-body">
            </div>
        </div>
    </div>
</div>

